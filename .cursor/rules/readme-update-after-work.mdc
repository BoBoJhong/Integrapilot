---
description: 功能或架構變更後同步更新 README.md，方便開發者掌握專案
alwaysApply: true
---

# README.md 維護慣例（IntegraPilot 專案）

當你完成會影響**開發者如何理解或使用專案**的變更時，**在結束該次工作前**應檢查並更新專案根目錄的 **`README.md`**，使內容與程式一致。

## 建議更新時機

- 新增／移除主要功能（例如 API、前端流程、CLI 參數）
- 變更 **專案結構**（新目錄、重要檔案、建置產物路徑）
- 變更 **環境變數**、**Docker** 用法、**安裝步驟**或**依賴**
- 變更對開發者重要的**工具鏈**（Node、Pandoc、掛載路徑等）

## 建議維持的 README 區塊（可依實際調整標題）

1. **專案簡介與特色**（一句話 + 條列重點）
2. **專案結構**（目錄樹或表格）
3. **環境需求與安裝**
4. **環境變數**（含選填項與說明）
5. **使用方式**：CLI、本機 Web、Docker（含範例）
6. **API 或功能摘要**（端點或模組名稱）
7. **常見問題**（與路徑、Docker、金鑰相關）

若變更只改註解或重構且無行為差異，可省略 README。

完成後簡短回報「已更新 README 的哪些段落」。
